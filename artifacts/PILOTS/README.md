# Pilot Runs

This folder holds historical pilot, development, and regression runs.

Use it when you need to inspect how the Step 1 URL-discovery process evolved.
Do not treat these folders as current production handoffs.

Current production-facing outputs live in:

```text
policy_scraper/artifacts/PIPELINE_OUTPUTS/
```

Detailed non-pilot audit evidence lives in:

```text
policy_scraper/artifacts/AUDIT_TRAILS/
```
