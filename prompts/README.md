# Prompt Templates

Prompt templates are versioned inputs for controlled AI workflow stages.

Current placeholders:

- `catalog_discovery_v0.md`: candidate catalog/source discovery for difficult cases.
- `policy_classification_v0.md`: policy classification from bounded catalog excerpts.

Do not silently edit a prompt during a full-scale run. If a prompt changes, create a new version or document which records used each version.
